import { StyleSheet } from 'react-native'
import { errorColor, tinyTextSize } from '../../../assets/styles'

export default StyleSheet.create({
  text: {
    color: errorColor,
    fontSize: tinyTextSize,
    marginBottom: 12,
    paddingLeft: 12,
  },
})
